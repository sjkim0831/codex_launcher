#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
HOST="${CARBONET_CODEX_HOST:-localhost}"
PORT="${CARBONET_CODEX_PORT:-43110}"

exec python3 "$SCRIPT_DIR/app/server.py" \
  --app-root "$SCRIPT_DIR" \
  --host "$HOST" \
  --port "$PORT" \
  "$@"
