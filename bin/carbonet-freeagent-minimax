#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export FREEAGENT_PROVIDER=minimax
export FREEAGENT_MODEL="${FREEAGENT_MODEL:-minimax2.7}"

exec "$SCRIPT_DIR/bin/carbonet-freeagent" "$@"
